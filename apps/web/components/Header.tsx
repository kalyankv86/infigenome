'use client';
import Link from 'next/link';
import {Menu,X} from 'lucide-react';
import {useState} from 'react';
export default function Header(){const [open,setOpen]=useState(false);const links=[['Home','/'],['About','/about'],['Services','/services'],['Blog','/blog'],['Contact','/contact']];return <header className="sticky top-0 z-50 border-b border-slate-100 bg-white/95 backdrop-blur"><div className="container flex h-20 items-center justify-between"><Link href="/" className="flex items-center gap-3"><img src="/images/cenomics-logo.png" alt="Infigenome" className="h-12 w-auto object-contain"/><span className="hidden font-bold tracking-tight text-slate-700 sm:block">INFIGENOME</span></Link><nav className="hidden items-center gap-8 md:flex">{links.map(([n,h])=><Link key={h} href={h} className="text-sm font-semibold text-slate-600 transition hover:text-sky-700">{n}</Link>)}<Link href="/contact" className="btn btn-primary text-sm">Start a conversation</Link></nav><button className="md:hidden" onClick={()=>setOpen(!open)} aria-label="Toggle menu">{open?<X/>:<Menu/>}</button></div>{open&&<nav className="border-t bg-white px-5 py-4 md:hidden">{links.map(([n,h])=><Link onClick={()=>setOpen(false)} className="block py-3 font-semibold" key={h} href={h}>{n}</Link>)}</nav>}</header>}
